return {
  {
    "tpope/vim-commentary",
    config = function()
      -- Maps:
      -- `gc` to comment out target of a motion (highlight stuff, gc to comment it all out)
      -- `gcc` comment out a line
    end
  }
}
